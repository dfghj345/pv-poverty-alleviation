# ==========================================
# 光伏扶贫项目综合依赖 (Backend API + Data Pipeline)
# ==========================================

# 1. Web 框架与 ASGI 服务器 (FastAPI)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# 2. 数据库与地理空间扩展 (PostGIS)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.28.0
geoalchemy2>=0.14.0  
psycopg2-binary>=2.9.0

# 3. 缓存与队列状态 (Redis)
redis>=5.0.0

# 4. 异步任务队列 (Taskiq - 替代 Celery)
taskiq>=0.11.0
taskiq-aiopg>=0.1.0

# 5. 数据抓取与解析 (Playwright + BS4)
playwright==1.40.0   # 必须严格等于 1.40.0，以匹配 Docker 底包内核
httpx>=0.25.0        # 异步请求
beautifulsoup4>=4.12.0
lxml>=4.9.0          # BS4 的底层 C 语言解析加速器

# 6. 数据处理
pandas>=2.0.0

# 7. 配置与环境变量管理
pydantic>=2.0.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0